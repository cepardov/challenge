app: ms-products

spring:
  datasource:
    url: jdbc:mariadb://${MARIADB_HOST:mariadb}:3306/products_db
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect